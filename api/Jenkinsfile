pipeline {
    agent any

    environment {

    DOCKERHUB_CREDENTIALS = credentials('akhil')
     registry = "yellaiahdocker/lms-api"
        registryCredential = 'akhil'
        dockerImage = ''
    }

    stages {

        stage('Building the docker image') {
            steps {
                sh 'cd api && docker build -t yellaiahdocker/lms-api .'
            }
        }
        stage('Logging into dockerhub account') {
            steps {
                sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
            }
        }
        stage('pushing the docker image into dockerhub') {
            steps {
                  sh 'docker push yellaiahdocker/lms-api'
            }
        }
        stage('Remove old docker images') {
             steps {
                 sh 'docker rmi -f yellaiahdocker/lms-api'

            }
        }
         stage('Running on kubernetes') {
            steps {
                echo 'Deploying....'
                sh "cd api && kubectl apply -f postgres-deployment.yaml"
                 sh "cd api && kubectl apply -f postgres-cluster-ip-service.yaml"
                sh "cd api && kubectl apply -f api-deployment.yaml"
                sh "cd api && kubectl apply -f api-load-balancer-service.yaml"
                sh "kubectl rollout restart deployment api-deployment"
            }
        }
    }
}
