pipeline {
    agent any

    stages {
         stage('Test') {
            steps {
                echo 'Sonar Analysis'
                sh 'cd webapp && sudo docker run  --rm -e SONAR_HOST_URL="http://54.91.206.117:9000" -e SONAR_LOGIN="sqp_b5fa24ef380116592dc68d8699112697088bfa26"  -v ".:/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms'
            }
        }    

    }
}
