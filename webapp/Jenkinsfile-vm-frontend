pipeline {
    agent any

    stages {
        stage('Building the code') {
            steps {
                echo 'Building..'
                sh 'cd webapp && npm install'
             

            }
        }

        stage('Deploying the code into nginx server') {
            steps {
                echo 'Deploying....'
                  sh 'cd webapp && sudo cp -r /var/lib/jenkins/workspace/lms/webapp/dist/*  /var/www/html'
            }
        }
    }
}
